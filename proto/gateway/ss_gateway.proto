syntax = "proto3";

package million.gateway.ss;

import "module/module_id.proto";

enum ServiceNameId {
    option (module.module_id) = MODULE_ID_GATEWAY;

    SERVICE_NAME_ID_INVALID = 0;
    SERVICE_NAME_ID_GATEWAY = 1;
}
