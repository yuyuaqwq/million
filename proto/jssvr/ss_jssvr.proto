syntax = "proto3";

package million.jssvr.ss;

import "module/module_id.proto";

enum ServiceNameId {
    option (module.module_id) = MODULE_ID_JSSVR;

    SERVICE_NAME_ID_INVALID = 0;
    SERVICE_NAME_ID_JS_CONFIG = 1;
}
